{
  "name": "imba-vite-project",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pages": "npx gh-pages --no-history --dotfiles --dist dist --branch release",
    "publish": "npm run build && npm run pages",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@testing-library/dom": "^8.17.1",
    "@testing-library/jest-dom": "^5.16.5",
    "@vitest/ui": "^0.23.1",
    "imba": "^2.0.0-alpha.220",
    "jsdom": "^20.0.0",
    "vite": "^3.1.0",
    "vite-plugin-imba": "^0.4.1",
    "vitest": "^0.23.1",
    "vitest-github-actions-reporter-temp": "^0.8.3"
  },
  "dependencies": {
    "marked": "^4.1.0"
  }
}
