{
  "name": "imba-vite-project",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pages": "npx gh-pages --no-history --dotfiles --dist dist --branch release",
    "publish": "npm run build && npm run pages",
    "preview": "vite preview"
  },
  "devDependencies": {
    "imba": "^2.0.0-alpha.223",
    "jsdom": "^20.0.1",
    "vite": "^3.1.6",
    "vite-plugin-imba": "^0.9.0"
  },
  "dependencies": {
    "highlight.js": "^11.6.0",
    "marked": "^4.1.1",
    "prismjs": "^1.29.0"
  }
}
